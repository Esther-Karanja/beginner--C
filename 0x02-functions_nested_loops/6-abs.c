#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: integer input
 *
 * Return: always 0 (success)
*/
int _abs(int r)
{
	if ((r > 0) || (-1(r < 0)))
	{
		_putchar (r);
		return (0);
	}
}
