#include "main.h"

/**
 * void print_alphabet(void);
 *
 * print the alphabet in lowercase letters
 *
 * main - check the code
 *
 * Return: Always 0.
*/
int main(void)
{
	char c = 'a';
	/*print lowercase letters*/

	for (c = 'a'; c <= 'z'; c++)

	print_alphabet();
	return (0);
}
void print_alphabet(void)
{
	_putchar('c = 'a'; c <= 'z'; c++');
	_putchar('\n');
}
